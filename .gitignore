satellite-ai-saas/
dist/
outputs/
node_modules/

# Local Obsidian ↔ GitHub sync configuration and logs
automation/obsidian_sync_config.json
automation/logs/

__pycache__/
*.pyc

deploy_result.json
deploy_stdout.txt

# 푸시용 임시 배치와 로그.
# 셸에서 커밋을 못 만들 때 .bat 을 써서 실행 창으로 돌린다. 그 부산물이
# 저장소 루트에 쌓여 git status 가 200줄씩 나왔다. 진짜 변경이 묻힌다.
*.bat
COMMIT_MSG.txt
push*.txt
push*.log
*_log.txt
_bak/
_bak2/
_trash/

# 조사용 일회성 스크립트와 출력. 밑줄로 시작하면 올리지 않는다.
scripts/_*.py
zz*.md
zz*.txt
COMMIT*.txt


# 비전에 보내려고 긴 상세 이미지를 잘라둔 조각. 매 실행마다 다시 만든다.
data/daiso_real/gosi_slice/

# 대한화장품협회 표준화명칭목록 원본 PDF (11MB). 사전을 만들 때만 쓰고
# 결과인 data/inci_dictionary.json 만 올린다.
data/daiso_real/kcia_std_names.pdf

# 법률 책임자 실명·주소·연락처와 승인 증적은 공개 저장소에 올리지 않는다.
# 예시 파일(*.example.json)만 공유한다.
data/manual/legal_responsible_person.json
data/manual/legal_product_overrides.json
data/manual/shopify_action_approvals.json
