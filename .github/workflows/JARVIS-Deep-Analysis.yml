name: 🧠 JARVIS Deep Analysis (매 1시간)

on:
  schedule:
    - cron: '0,30 * * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  deep-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4.1.7
        with:
          fetch-depth: 0

      - name: 🐍 Python 3.10 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 라이브러리 설치
        run: |
          pip install requests feedparser torch numpy matplotlib scikit-learn --break-system-packages

      - name: 🧠 [1/3] MoE 신경망 훈련
        id: moe_training
        run: |
          echo "🧠 MoE 라우터 학습..."
          if python scripts/moe_training.py; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

      - name: 📡 [2/3] arXiv 논문 수집
        id: arxiv_collect
        run: |
          echo "📡 arXiv MoE 논문 수집..."
          if python scripts/collect_moe_papers.py; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

      - name: ❤️ [3/3] 시스템 헬스체크
        id: health_check
        run: |
          echo "❤️ JARVIS 시스템 상태 확인..."
          echo "status=success" >> $GITHUB_OUTPUT

      - name: 📤 GitHub 자동 푸시
        run: |
          git config user.name "JARVIS"
          git config user.email "jarvis@automated.local"
          git add data/ index.html
          git commit -m "🧠 JARVIS Deep: MoE훈련+arXiv수집+헬스체크 (매 1시간)" || echo "No changes to commit"
          git push origin main || echo "⚠️ Push failed"

      - name: ✅ 완료 로그
        if: always()
        run: |
          echo "🎯 JARVIS Deep Analysis 완료"
