거짓말 데이터 금지

가짜 데이터 금지

작업할때 항상 파이썬 스크립트 있는지 확인

push 는 JARVIS가 무조건 작업하고 성공여부 보고

github 는 JARVIS가 무조건 push 하고 성공여부 보고 

반드시 확인하고 보고할것


## 다이소 고시는 img 의 alt 에 있다 (2026-09-10)

식물원 병풀 클리어 앰플(pdNo 1041749)로 확인했다.

  고시 표는 이미지다. 그런데 그 전문이 img 의 alt 속성에 글자로 있다.
  실측 1,077자. 제품명·용량·제조국·전성분·주의사항·품질보증·상담전화가 다 있다.
  줄바꿈 60개로 나뉜 표 형태다.

  ※ 아래 두 줄은 틀렸다. 2026-09-13 에 실측으로 뒤집었다. 밑에 정정을 적었다.
  ~~그 alt 는 상품 페이지 HTML 안에 그대로 있다.~~
  ~~collect_daiso.py 가 이미 받아오는 그 HTML 이다. 추가 요청이 필요 없다.~~

화면에서 그것이 어디 있냐면 "상품설명 더보기" 버튼 아래다. (2026-09-13 재확인)

  상품 페이지를 열면 설명이 접혀 있다. 가운데에 이런 버튼이 하나 있다.

      +-------------------------------------------+
      |            상품설명 더보기  v              |
      +-------------------------------------------+

  그 버튼을 누르면 제품 상세가 더 나오고 고시 표도 다 나온다.
  스크롤을 내리면 끝까지 보인다. 사용자가 화면으로 보고 있는 것이 이것이다.

  중요한 것은 이거다. 접혀 있다고 해서 HTML 에 없는 게 아니다.
  접힌 상태로 이미 HTML 안에 들어 있다. 버튼은 펼쳐 보여줄 뿐이다.
  그래서 collect_daiso.py 가 받아오는 HTML 에 그대로 있다.
  클릭을 흉내 낼 필요도, 추가 요청을 보낼 필요도 없다.

  전에 "본문에 없다" 고 두 번 보고했다. 둘 다 이것 때문이었다.
  접혀 있어서 innerText 에 안 잡혔고, 표는 이미지라 alt 에 있었다.
  두 겹으로 숨어 있었던 것이지 없었던 게 아니다.

## 정정 — HTTP 로 받은 HTML 에는 고시가 없다 (2026-09-13)

사용자가 "다이소제품은 고시가 없는게 없어 네가 못봤거나 실수한거야" 라고 했다.
맞았다. 위에 내가 "HTML 안에 그대로 있다" 고 적어 둔 것이 틀렸다.

HTTP 로 받은 원본을 상품 3개로 실측했다. 전부 같다.

  HTTP 200 · HTML 43,799자
  img alt 총 12개 · 200자 넘는 것 0개
  /description/ 이미지 URL 0개
  lazy 속성 0개
  editor-content 영역이 HTML 에 없다

editor-content 자체가 원본에 없다. 스크립트가 그린다.
그래서 requests 로 받아서는 alt 를 절대 못 본다.
브라우저에서 스크롤해야 나타난다. 그때 1,077자가 보인다.

고시가 있는 자리는 두 곳이고 규칙은 단순하다.

  ① 「상품정보 제공 고시」 표
     div.el-collapse-item__header 접힌 칸. 11항목.
     모든 제품에 있다. 그런데 대부분 "상세페이지 참조" 다.
     제조국만 실값("한국")이다. 이게 selPdDtlNtfc 와 같은 값이다.

  ② img 의 alt  ← 실제 내용은 여기
     div.cms > div.editor-area > div.editor-content 안.
     이미지 src 경로에 /description/ 이 들어간다.
     ①이 "상세페이지 참조" 라고 가리키는 그 상세페이지가 이것이다.
     제품명·용량·제조국·제조업자·전성분·사용법·주의사항이 다 있다.

확인한 것
  1041749 식물원 병풀 클리어 앰플   alt 1,077자 · 줄바꿈 60
  1053482 메디필 모공 진정 토너      alt 있음

그래서 수집 방법이 바뀐다
  이미지를 내려받아 Gemini 비전으로 읽을 이유가 없다.
  alt 가 이미 글자다. 렌더링된 DOM 에서 꺼내면 끝이다.
  URL 담는 수집기도, GEMINI_API_KEY 도 필요 없다.
  필요한 것은 헤들리스 브라우저 하나다. Actions 에서 무료로 돈다.

내가 왜 틀렸나
  한 번 봤을 때 alt 를 찾았고, 그것이 HTML 에 있는 줄 알았다.
  브라우저 개발자도구로 본 것을 HTTP 응답과 같다고 여겼다.
  둘은 다르다. 개발자도구는 렌더링 후, requests 는 렌더링 전이다.
  다음부터 "HTML 에 있다" 고 적을 때는 requests 로 받아서 세어 본 뒤 적는다.


여기는 이미 확인이 끝났다. 다시 파지 않는다.
  selPdDtlDesc 의 pdDtlDc     309자 평문. 고시 없음.
  selPdDtlDesc 의 pdDtlNoti   빈 배열.
  selPdDtlNtfc                11항목 중 3개만 실값. 용량·전성분은 "상세페이지 참조".
  /banner/ 경로 이미지          광고 슬라이드(ad-img-box). 상세 이미지 아님.
  /description/ 경로 이미지     이것이 상세 이미지다. alt 에 고시 전문이 있다.


## "없다" 고 말하기 전에 세 곳을 본다

위 건에서 세 번 틀렸다. 전부 덜 보고 단정한 것이다.

  1차  API 응답 한 곳만 보고 "텍스트로는 어디에도 없다" 고 적었다
  2차  브라우저 innerText 만 훑고 "본문에 0건" 이라고 했다
       alt 속성은 innerText 에 안 들어간다. 그래서 안 보였다.
  3차  이미지 82장을 상세 이미지로 착각했다
       DOM 부모를 안 따라갔다. 따라가 보니 전부 광고였다.

그래서 규칙으로 둔다. 화면에 보이는데 코드가 못 찾으면 순서대로 본다.

  innerText  ->  innerHTML  ->  모든 속성(alt·title·data-*)  ->  DOM 부모 사슬

세 번째까지 안 보고 "없다" 고 보고하지 않는다.
사용자가 화면으로 보고 있는 것을 내가 못 찾는 것이면, 없는 게 아니라
내가 안 본 자리에 있는 것이다.


## 커밋 전에 로컬이 원격보다 뒤인지 본다 (2026-09-10)

로컬 작업 폴더가 원격보다 한참 뒤에 있었다. 수정 104개, 추적 안 됨 204개.
그 상태에서 파일을 고쳐 커밋했더니 8개 파일에 1,575줄 추가 / 387줄 삭제가
나왔다. 실제 수정은 그보다 훨씬 작았다. 차액은 전부 낡은 로컬로 원격을
덮는 부분이었다.

그날 하루 종일 고친 것이 정확히 그 버그였다. 워크플로 아홉 개가 같은
방식으로 남의 최신 결과를 되돌리고 있었다.

  커밋을 만들기 전에 git fetch 하고 원격과의 차이를 먼저 본다.
  뒤에 있으면 git reset --hard origin/main 으로 맞춘 다음 수정을 다시 얹는다.
  git diff --stat 이 내가 고친 것보다 크면 멈추고 왜 큰지 본다.


## 판단을 뒤집으면 근거를 코드에 남긴다

틀린 판단과 그것을 뒤집은 실측값을 주석에 같이 적는다.
다음에 같은 자리를 파는 사람이 헛돌지 않게 한다.
