# 🤖 [JARVIS SYSTEM] YouTube Tech Insight: AI Infrastructure Explained (GPUs, vLLM, and LLM-D)

## 1. 개요 (Overview)
* **영상 제목:** AI Infrastructure Explained (GPUs, vLLM, and LLM-D)
* **채널명:** KodeKloud
* **핵심 주제:** AI 인프라의 핵심 구성 요소(GPU, VRAM, vLLM, LLM-D 및 쿠버네티스 오케스트레이션)에 대한 엔지니어링 관점의 해부

---

## 2. 핵심 요약 및 기술 스펙 (Technical Breakdown)

### ① 모델(Model)과 연산 구조
* **모델의 본질:** 트랜스포머(Transformer) 아키텍처라는 고정된 소스코드(공식)와, 수십억~수백억 개의 가중치(Weights) 파일 조합
* **토큰(Token):** LLM이 읽고 쓰는 언어의 기본 단위 (평균적으로 1단어는 약 1.33개의 토큰, 약 3/4 단어 크기)
* **추론 루프 (Inference Loop):** 프롬프트를 입력받아 다음 토큰을 1개씩 예측하여 생성하는 루프를 반복

### ② CPU vs GPU 및 메모리 아키텍처
* **CPU:** 복잡한 직렬 처리에는 탁월하나 대규모 병렬 연산에는 한계 존재
* **GPU와 VRAM:** 대규모 병렬 연산을 수행하기 위해 전용 온보드 메모리(**VRAM**) 장착. 테라바이트(TB) 급 대역폭으로 모델 가중치를 코어에 공급
* **성능 지표 3가지:** 
  * Compute (연산 능력, TFLOPS)
  * Capacity (VRAM 용량, GB)
  * Bandwidth (메모리 대역폭, TB/s)

### ③ vLLM 및 캐싱 메커니즘
* **vLLM:** 파이토치(PyTorch) 기반 위에서 모델을 웹 API 서비스 형태로 구동하는 고성능 모델 서버
* **KV Cache (Key-Value Cache):** 프롬프트 처리 단계(Prefill)의 연산 결과를 저장해 두어, 매 토큰 생성 시마다 프롬프트를 다시 읽지 않고 재사용하는 스크래치패드
* **Prefix Caching:** 대화 히스토리나 공통 시스템 프롬프트의 연산 결과를 캐싱하여 대화가 길어져도 비용과 지연 시간을 대폭 절감
* **Batching (배치 처리):** 여러 사용자의 요청을 묶어서 단 한 번의 모델 가중치 로드만으로 다중 토큰을 동시 처리하여 처리량(Throughput) 극대화

### ④ 대규모 분산 및 LLM-D 오케스트레이션
* **샤딩 (Sharding):** 단일 GPU에 들어가지 않는 거대 모델을 여러 GPU 및 노드에 쪼개어 배치하는 기술
* **LLM-D:** Red Hat, Google, IBM, Nvidia 등이 협력하여 개발한 오픈소스 AI 인프라 라우터 및 오케스트레이션 도구
* **프리필/디코드 분리 (Prefill vs. Decode Split):** 연산 위주의 프리필 풀과 메모리 대역폭 위주의 디코드 풀을 물리적으로 분리하여 병목 현상 해결
* **Kubernetes 통합:** 일반적인 로드 밸런서의 한계를 극복하고, 캐시 인지형(Cache-aware) 라우팅과 리더-워커셋(Leader Worker Set)을 활용한 안정적인 클러스터 운영

---

## 3. 관련 링크 및 레퍼런스
* [AI Infrastructure Explained 동영상 링크](https://www.youtube.com/watch?v=hBzUokVYQkI)
* [LLM-D 공식 프로젝트 사이트](https://llm-d.ai)
* [KodeKloud AI 러닝 패스](https://kodekloud.com/learningpath/ai)