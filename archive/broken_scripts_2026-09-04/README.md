# 실행 불가 스크립트 격리 (2026-09-04)

preflight 검증에서 문법 오류로 걸린 파일들. 삭제하지 않고 원본 그대로 보관한다.
두 파일 모두 워크플로·스크립트·배치 어디에서도 참조되지 않아 자동화에 영향이 없다.

## JARVIS_Phase27_신경망아키텍처_세부설계.py.txt

설계 메모를 `.py` 확장자로 저장한 파일. Python 문법이 아니라 처음부터 실행된 적이 없다.

    "shape": (timesteps=100, features=8),

키워드 인자를 튜플 안에 쓴 형태라 파싱 자체가 불가능하다. 내용은 신경망 아키텍처
설계 기록이므로 문서로 보존한다.

## setup_github_secrets.py.broken

한글 문자열이 인코딩 손상(mojibake)되어 제어문자 18개가 섞였고 그 때문에 문법이
깨졌다. GitHub Secrets 등록 안내를 출력하던 스크립트로, 필요한 Secrets는 이미
저장소에 등록을 마쳐 용도가 없다.

복구하려면 손상된 문자열 리터럴을 다시 작성해야 하며, 원본 한글은 복원 불가다.
